export const MENTOR_GITIGNORE = `# Mentor Studio Code — runtime files. Do not commit.
# The DB and other per-developer files now live in OS-managed user data
# (see CHANGELOG v0.6.6). Keep this directory out of git by default.
*
!.gitignore
`;
