{
  "name": "mentor-studio-code",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "extension",
    "extension/webview"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present"
  }
}
