{
  "name": "mentor-studio-code",
  "private": true,
  "workspaces": [
    "packages/*",
    "extension",
    "extension/webview"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present"
  }
}
